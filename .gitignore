# Dependencies
node_modules/

# Build output
dist/
backend/dist/
backend/build/

# Kokoro model files (too large for git; downloaded during CI build)
backend/models/

# PyInstaller spec (regenerated each build)
backend/*.spec

# Python artifacts
*.pyc
__pycache__/

# Environment / secrets
.env

# OS cruft
.DS_Store

# NOTE: the following are intentionally tracked (do NOT ignore):
#   backend/      (source files)
#   electron/
#   assets/
#   package.json
#   .github/
